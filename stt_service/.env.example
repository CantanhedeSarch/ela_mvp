# ================================================
# Configuração de Ambiente - Microsserviço STT
# ================================================
# Copie este arquivo para .env e ajuste os valores
# Para ativar: source .env (Linux/Mac) ou execute linha por linha (Windows)
# ================================================

# ==========================================
# OBRIGATÓRIO: Modelo Vosk
# ==========================================
# Caminho absoluto para o diretório do modelo Vosk pt-BR
# Baixe de: https://alphacephei.com/vosk/models
# Exemplo Windows: C:\projetos\ela_mvp\models\vosk-model-small-pt-0.3
# Exemplo Linux: /home/user/models/vosk-model-small-pt-0.3

VOSK_MODEL_PATH=models/vosk-model-small-pt-0.3


# ==========================================
# Integração com Serviço de Glossa
# ==========================================
# URL completa do endpoint de tradução para glossa
# Se estiver rodando o professional_api.py localmente na porta 9000:

GLOSSA_SERVICE_URL=http://localhost:9000/traduzir

# Timeout para requisições ao serviço de glossa (segundos)
GLOSSA_TIMEOUT=10


# ==========================================
# Configurações do Servidor
# ==========================================
# Porta onde o serviço STT irá escutar

STT_PORT=9100

# Origens permitidas para CORS (separadas por vírgula)
# Use "*" para aceitar qualquer origem (apenas desenvolvimento!)
# Em produção, especifique domínios: https://app1.com,https://app2.com

STT_ALLOWED_ORIGINS=*


# ==========================================
# Configurações de Áudio
# ==========================================
# Taxa de amostragem do áudio (Hz)
# Vosk pt-BR funciona melhor com 16000Hz (não altere sem necessidade)

# STT_SAMPLE_RATE=16000

# Tamanho do buffer de áudio para processamento (bytes)
# 8192 bytes = ~256ms de áudio em 16kHz mono PCM16
# Valores maiores: menos overhead, mais latência
# Valores menores: mais overhead, menor latência

STT_BUFFER_SIZE=8192


# ==========================================
# Configurações de Sessão WebSocket
# ==========================================
# Tempo máximo de inatividade antes de fechar sessão (segundos)

STT_SESSION_TIMEOUT=300

# Intervalo mínimo entre emissões de transcrições parciais (segundos)
# Evita flooding do cliente; 0.5s é um bom equilíbrio

STT_PARTIAL_INTERVAL=0.5


# ==========================================
# Configurações de Logging
# ==========================================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL

STT_LOG_LEVEL=INFO

# Diretório para arquivos de log (relativo ao diretório do script)

STT_LOG_DIR=logs


# ==========================================
# Comandos para ativar (copie e execute)
# ==========================================

# Windows PowerShell:
# $env:VOSK_MODEL_PATH = "models/vosk-model-small-pt-0.3"
# $env:GLOSSA_SERVICE_URL = "http://localhost:9000/traduzir"
# $env:STT_PORT = "9100"
# $env:STT_LOG_LEVEL = "INFO"

# Linux/Mac Bash:
# export VOSK_MODEL_PATH="models/vosk-model-small-pt-0.3"
# export GLOSSA_SERVICE_URL="http://localhost:9000/traduzir"
# export STT_PORT="9100"
# export STT_LOG_LEVEL="INFO"

# ================================================
